PORT=4001

# Required because the socket provider owns playback and refreshes Spotify tokens.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

# Comma-separate origins when you need both local and production clients.
CORS_ORIGIN=http://127.0.0.1:3000
